import { Box } from '@mantine/core';
import { ProductsFeedFeature } from '../../features';

const StoreScreen = () => {
  return (
    <Box>
      <ProductsFeedFeature />
    </Box>
  );
};

export default StoreScreen;
